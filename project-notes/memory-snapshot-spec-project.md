---
name: snapshot-spec-project
description: "Months-long project — reverse-engineer a snapshot-generation spec doc from .NET + Java implementations; plan, decisions, materials locations, phase status"
metadata: 
  node_type: memory
  type: project
  originSessionId: e358eab0-d562-4968-9936-dca1f9c7680c
  modified: 2026-09-09T14:58:53.876Z
---

# Snapshot-generation reverse-engineering project

## Session protocol (multi-session work)

- **Work unit = one packet per session**, sized to finish without context compaction: one chapter's deep-read (Phase 2/3), one harness milestone (Phase 4), one adjudication batch (Phase 5). Phases are too big for single sessions; packets are the subdivision.
- **Session start:** read this file → `docs/snapshot-spec/README.md` status table → the NEXT UP line below. The user only needs to say "continue the snapshot spec project".
- **Session end (invariant):** every insight is externalized — document sections filled (never left only in conversation), ch13/ch14/ch15 updated, this file's phase status + NEXT UP updated, **and this file re-copied to `C:\Git\snapshot-spec-materials\project-notes\memory-snapshot-spec-project.md`, then `git commit` in `C:\Git\snapshot-spec-materials`** (that directory is a git repo since 2026-09-09 — tracks extracts/harness code/repros/R6-build HTML/project-notes, ignores clones/tools/out; Ewout adds the remote and pushes. The `.claude` folder is swept when the project idles; `project-notes\README.md` explains how to restore memory + plan from there). Use subagents for bulk reading; keep their outputs as durable extract files in `C:\Git\snapshot-spec-materials\extracts\`.
- **Tell the user when to restart (user request, 2026-08-21):** when a packet completes, explicitly say so and recommend starting a fresh session (after updating NEXT UP). Also proactively recommend a restart mid-session if context is filling up or the next packet is a different kind of work. Never let the user guess when a restart is safe — say "this is a clean restart point" or "keep going, packet unfinished".
- **NEXT UP: nothing pre-WGM is left except Ewouts own read of the brief.** ALL standing carry-ins CLOSED 2026-09-03 (packet WGM-4): DEV-029 default-settings .NET re-run done (throws settings-independent; Q15 caveat removed — a post-freeze factual correction), DEV-033 minimized repro built (harness/repros/dev033b-input.xml) and posted as a comment on hapifhir#2584, JI-18 verified = CRASH (NPE PU:2611, complex obligation ext) → filed **#2602**, JI-14 verified (+ rename lands in 
ame) → **#2603**, JI-19 verified (ValidationEngine never calls setXver) → **#2604**, NEW JI-21 (extension slicing entry w/o explicit ordered=false → whole-profile preprocessor bail-out) → **#2605**; DEV-031 traced empirically: children of a Base-typed new element hoisted to the grandparent (type snapshot without children; kind- and position-independent), exact line needs a debugger; .NET issue FILED as firely-net-sdk#3597 (Ewout 2026-09-03: eel free to file .net issues too = STANDING AUTHORIZATION for .NET snapshot-generator issues as well, same rules as Java: verified only, dup-check first). Harness gained --defaults (.NET SDK default settings) and epros/ + JavaRunner stack traces; org.hl7.fhir.core master fetched = f725fb7 (PU +2 lines from :126). Held Java items: JI-15/16 (WGM Tier 3 design questions), JI-20 (needs custom-resource repro). ch1 overview refresh DONE 2026-09-03 (.NET/Java architecture sections + R6 note + state-of-study/reading guide). Optional leftovers, none WGM-blocking: 1-page handout (exec summary suffices), DEV-031 debugger session (#3597). POST-WGM: adjudication packets (fold outcomes into ch13/14/15; Ewout posts to FHIR-31405 + Zulip; re-verify RFCs vs R6 before Confluence). Previous NEXT UP (kept): brief is FROZEN (v3, 2026-09-03). Remaining pre-WGM options (each a small session, pick by Ewouts preference): (a) standing carry-ins that would sharpen demos — DEV-029 default-settings .NET re-run (removes the Q15 caveat), DEV-033 minimized repro, JI-18 repro, JI-14 repro, JI-19 caller check; (b) a 1-page printable handout = the exec summary alone (optional); (c) chapter polish (ch1 overview refresh, cross-links) — may slip past the WGM. Post-WGM: Phase 5 adjudication packets — fold WGM outcomes into ch13/ch14/ch15, Ewout posts to FHIR-31405 + his Zulip thread (Claude never posts), re-verify RFCs against R6 text before Confluence. **v3 DONE 2026-09-03:** exec summary re-read against Q1–Q16 (no drift; one line-break fix), Q10/Q14–Q16 kept in Tier 2 body with an if-time note under the Tier 2 header, 10 more permalinks browser-verified (22 total, stream+channel forms), status set FROZEN, README row 5 updated. **v2 DONE 2026-09-03** (commits 9c48769c4 + f1e26a114 on docs/snapshot-spec; ch14 OQ-018/019 re-tiered too): executive summary at the top (10 live decisions table + confirm-only list + if-time + after-the-session routing); evidence tiers marked (resolution vs comment vs Zulip agreement) after REST re-reads of 16 tickets — HL7 JIRA resolution text = REST field customfield_10618 (anonymous GET, browser UA; recipe + verbatim results in extract jira-sweep-2026-09-02.md §8); findings folded in: FHIR-28441 classification list is INCORPORATED BY REFERENCE into the resolution (Java omits NINE of the 17 class-5 urls, not five — extract §4/§6 said five, corrected in §8), FHIR-15900 = auto-approved tooling ticket (Q4b now has no decision-tier source on either side), FHIR-50267 rule = description adopted by Do this, Lloyd/Grahame lines = comments; Q2 tension bullet harmonized with the Zulip paragraph (two halves of one 2025 outcome); Q7c/Q3a/Q11b/Q13-mapping-inheritance re-labelled Zulip-only in Appendix B verdict summary; Q12 recommendation cross-refs Q5 and flags the cap-to-1 carve-out as beyond FHIR-28619; Q13 duplicate constraint.key rows merged; 12 Zulip permalinks browser-verified (stream+channel forms). Register fixups DONE: DEV-017 cites FHIR-34434, DEV-032 cites Lloyd 2024-11-20 near/483494571, DEV-021 Java side pinned (addInheritedElementsForSpecialization PU:1263-1283). Brief sent to Ewout via SendUserFile. **v3 tasks (small, one session):** re-read the exec summary once against Q1–Q16 for drift; decide whether Q10/Q14–Q16 stay in the body or move behind Tier 3; optionally verify the ~10 remaining Zulip permalinks; then set status to FROZEN and stop editing. Standing carry-ins unchanged (JI-18 repro, JI-19 caller check, JI-14 repro, DEV-029 default-settings .NET re-run, DEV-031 cdshooks trace, DEV-033 minimized repro); Zulip API key: regenerate when project ends. Previous NEXT UP (v2 plan, kept for reference): The brief exists: `docs/snapshot-spec/wgm-brief-2026-09.md` (draft v1, 2026-09-02, ~1130 lines, 16 questions in 3 tiers + Appendix A agenda index + Appendix B permalinks; all Zulip+JIRA sweep results folded in). v2 tasks: (1) coherence pass — the brief grew by accretion (draft → Zulip → JIRA folds). Advisor-flagged contradictions FIXED 2026-09-02 (Q8 decision now "both syntaxes sanctioned by id, is the fragment superseded?"; Q1 option C + recommendation restricted per FHIR-19756; Q1 sub-decision + Q3 FHIR-8969 tier-marked resolution-vs-comment; Q4 (b) states both sides [Grahame single-type rule vs FHIR-15900]; Q5 says it asks HL7 to reverse a uniform validator-warning precedent). STILL TO CHECK in v2: every remaining "Decision needed/Recommendation" block vs its later-added "Decisions on record (JIRA)" paragraph (Q2, Q3 a/b, Q7, Q9, Q11, Q12, Q13); tier-mark any other "decided" claim that rests on a JIRA comment rather than resolution text (the JIRA extract has R/C/X/O tiers per ticket). Verification state: 5 load-bearing JIRA quotes spot-checked against REST 2026-09-02 (FHIR-19756 resolution = "must fall within the cardinality bounds", "should not pull" = Chris Grenz COMMENT; FHIR-50267 decision text = Lloyd's description adopted by "Do this", "too late … validation warning" = Grahame comment; FHIR-12259 items 1/2/4 = description adopted "as proposed"; FHIR-13973 "appending a # and the id" = resolution verbatim; FHIR-28619 "*only* presently supported by type" = resolution verbatim) — the other ~75 tickets are agent-read only; Zulip permalinks: one `stream`-form and one `channel`-form link verified in the browser pane, rest unverified. (2) add a 1-page executive summary at the top (the evening session cannot consume 1100 lines — list the ~8 live decisions with the recommended answer each) and consider demoting answered items to a "confirm only" appendix; (3) spot-verify ~10 more permalinks in the browser pane (both `#narrow/stream/…` and `#narrow/channel/…` forms verified to resolve, one each); (4) decide what to do with FHIR-31405 (open, Waiting for Input since 2021 — natural place to post WGM outcomes on merge rules) and Ewout's live Zulip thread "Issue 13402 - Clarify snapshotting rules" (2026-09-01; Grahame replied; Gino Canessa posted an independent comparison https://ginoc.io/202609-snapshot/snapshot-generation-comparison.html — cited in the brief preamble); (5) the register fixups from the sweeps are DONE except: DEV-017 should cite FHIR-34434 (ED mapping same-identity REPLACES — neither engine implements), DEV-032 could cite Lloyd 2024 "reasonable to propagate obligations from root", DEV-021 Java side now pinned via ch9 (`addInheritedElementsForSpecialization`); (6) optionally send the brief to Ewout as a file (SendUserFile) once v2 is frozen. Sweep extracts (durable, read these not the raw APIs): `C:\Git\snapshot-spec-materials\extracts\zulip-sweep-2026-09-02-{a,b,c}.md`, `jira-sweep-2026-09-02.md`, `rfc-015-extension-url-fixed-2026-09-02.md`. Standing carry-ins unchanged: JI-18 repro, JI-19 caller check, JI-14 repro, DEV-029 default-settings .NET re-run (brief Q15 carries the caveat), DEV-031 cdshooks trace, DEV-033 minimized repro. Zulip API key: suggest regeneration when project ends. **Packet WGM-1 DONE 2026-09-02** (brief v1 written; 5 parallel subagents: 3 Zulip sweeps [~200 queries, ~70 threads], 1 JIRA sweep [~110 JQL, 83 tickets; REST works anonymously with a browser User-Agent], 1 RFC-015 research [632/632 extension-pack SDs author fixedUri; R6 build says derived complex-extension profiles don't establish the url → ext-sort-issue verdict retracted; defining-extensions §2.1.5.1.4 IS a partial spec basis]; RFC-015 written + RFC-016..019 added [contentReference absolutization, stale "complete verbose snapshots", ids derived, FHIR-50267 vs 50391 mustSupport contradiction]; ch14 statuses: answer-found for OQ-001 cardinality half [FHIR-19756/36738: root cardinality is a bound, "generator should not pull root cardinality into the snapshot"; FHIR-48664 2025 datatype-root binding = live obligation on both generators], OQ-003 [FHIR-28619 17-0-0 not allowed; Java carve-outs undiscussed], OQ-007 [SD.mapping propagates; Java fixed 2024; .NET to-do], OQ-008 [suppress = snapshot-time deletion, FHIR-31406, golden address-no-examples], OQ-009 [ids derived], OQ-015 [2019 decision: error not repair — both engines' defaults contradict it], OQ-017 syntax [BOTH url#id fragment (FHIR-13973) and profile-element (FHIR-49079) sanctioned, both by element id → .NET sliceName compare wrong, Java strip drops sanctioned form], OQ-018(c) [FHIR-12259; type slice is single-typed; .NET R5-form outlier], OQ-019 [FHIR-28441 class-5 list = .NET blocklist verbatim; inheritance-control metadata exists, unapplied], OQ-020(c)(d) [entry collapse + min raise unsanctioned: FHIR-12259/8969/31054]; DEV-023 flavor 1 = Java correct (Grahame 2022 absolutization rule); DEV-025 flavor 2 = FHIR-50391 outcome June 2025, and FHIR-50267 = the ONLY on-record algorithm rule ("slicer constraints not included in slice snapshots") ambiguous on child rows; DEV-038 empirical exhibit found (MyAddress demo-1 MWE, Zulip Oct-Nov 2025) + Java rationale (Grahame Nov 2024 "wasn't intended to work for types", "I don't actually know in that case"); OQ-002 verbatim re-raised by Ward Feb 2026 and Grahame proposed an evening meeting in Rotterdam = THIS WGM; Gino Canessa independent comparison corroborates DEV-024/027/020/028 at different pins. HL7 twice agreed to write the rules and abandoned it: FHIR-9079 [2016] + FHIR-13402 [2018] both Persuasive then reverted.) Previous NEXT UP (kept for reference): WGM question brief (Phase 5 start; freeze ~2026-09-14). Deliverable: self-contained markdown (suggest `docs/snapshot-spec/wgm-brief-2026-09.md`), per question: context, reproducing input/test id, .NET behavior, Java behavior, spec basis, decision needed; ordered by importance. Candidate top items (from register/OQs): DEV-038/OQ-001/OQ-002/OQ-021 (type-profile closure + materialization — the single biggest architectural fork), DEV-037/RFC-015 (Extension.url fixedUri), OQ-020/DEV-020 (type-slicing entry normalization), OQ-018 (renamed-form implicit type), OQ-011 + OQ-014 + DEV-028 (what must a generator enforce / error taxonomy / 21 fail tests), DEV-033 (Java copy-down bug blessed by golden), DEV-035 (orphan rows), OQ-017 (profile-element vs fragment), ch8 three-versions-three-behaviors (contentReference propagation R4/R5/R6), OQ-019 (non-inheritable lists), OQ-016 (differential-less SDs), OQ-009/015 (id regeneration + input mutation). Before freezing: sweep Zulip (`all` mode) + JIRA per question so nothing already-answered is asked; write RFC-015 properly; fold JI-15/16/18 design questions in as "ask Grahame" items. Standing carry-ins: JI-18 repro, JI-19 caller check (`git grep setXver` scoped to org.hl7.fhir.validation/src/main — avoid whole-tree grep on the blob:none clone, it times out), JI-20 custom-resource repro, DEV-038 sweep exhibit, DEV-029 default-settings .NET re-run (harness), DEV-031 cdshooks trace. Chapter polish (ch1 overview refresh, cross-links) may slip past the WGM. Run in a FRESH session. **Packet J-e DONE 2026-09-01 — PHASE 3 COMPLETE** (ch08–ch12 Java sections filled; extract java-ch08-12-sweep-2026-09-01.md; ch8: three path-dependent contentReference behaviors [step-in paths null ref + copy type = .NET; sliced-base PPP:1477 keeps ref + clears type = DEV-023 flavor 2; slicing-entry inline dump PPP:402-419 [the `else` of hasInnerDiffMatches: fires when the sliced entry has NO inner diff rows] keeps ref + materializes target children = DEV-025 flavor 1 = the only non-diff-driven expansion in Java; PPP:1477 type-clear hits only the last-emitted element of the sliced path], absolutization = generateIdForElement PU:4359 at setIds time over snapshot + caller's differential with hard-coded core prefix [JI-20], §5.1.0.10 propagation implemented by neither engine; ch9: type/derivation mandatory, specialization append PU:842-867 + addInheritedElementsForSpecialization, choice-group pruning checkGroupConstraints PU:1333 [Java-only, FHIRPath `(a|b)=1` invariants → other members max=0 + subtree removed, Errors], no interface handling in profile/context/conformance packages, imposeProfile only in targetProfile check, populateLogicalSnapshot has no caller; ch10: ids always regenerated [OQ-009 agreement], fixChars `_`→`-`, no [x] Base dependence, replacedIds dead pass, setIds mutates caller diff [OQ-015 CORRECTED]; ch11: snapshotStack of derived urls + SD generatingSnapshot flag + first-element rule PPP:714-724 [DEV-029 mechanisms pinned], five re-entry sites, Error not caught → half-built snapshot survives; ch12: message-list-or-throw ctor, gate bypass PPP:351/355, live throw census DefinitionException 67 / FHIRException 68 / java.lang.Error 30, settings table, four policy lists + snapshot-behavior, OQ-019 list diff 8/7/9). Previous NEXT UP (J-e plan, kept for reference): (Java ch08–ch12 sweep: ch08 contentReference = getElementById PU:3524 [url#id cross-SD resolution] + PPP redirector paths 858-896/1118-1156/403-419 [inline dump of contentReference target children in the slicer path] + replaceFromContentReference PU:1870; ch09 logical models/interfaces = specialization append PU:842-867 + addInheritedElementsForSpecialization 1263-1283 [type children copied under new elements], populateLogicalSnapshot 4546, `type` mandatory on all SDs PU:750-755 vs .NET repair, no interface skip in cone [check context loader outside cone: `git grep structuredefinition-interface HEAD -- org.hl7.fhir.r5/src/main/java/org/hl7/fhir/r5/context/`], checkTypeParameters 1159 [Java-only]; ch10 ids/Base = setIds PU:886 + generateIds ~4300, updateFromBase 2004, specialization Base fill 969-975, cloneSnapshot id rewrite 1493; ch11 recursion = snapshotStack 774-776 + isGeneratingSnapshot/checkNotGenerating 1694 + first-element-only rule PPP:719-724 + re-entrant generateSnapshot channels PPP:710/730, PU:2080/2638/767 + half-built snapshot nulled on exception 1078-1084 [contrast .NET SnapshotRecursionStack, ext-recursion-2/logical-goo crossover DEV-029]; ch12 errors/settings = exception-vs-message classes [DefinitionException/FHIRException/java.lang.Error/ValidationMessage ERROR/WARNING/INFO/log-only], wantThrowExceptions PU:1221-1226, allowUnknownProfile [ALL_TYPES default, #2597], autoFixSliceNames, newSlicingProcessing, forPublication, mappingMergeMode, suppressedMappings, parameters [version params in findProfile 4109-4139], EXT_SNAPSHOT_BEHAVIOR SD-level policy 1228-1261, snapshot-base-version stamp 1091-1093, NON_INHERITED_ED_URLS list [compare .NET's 17-url blocklist, OQ-019], makeExtensionForVersionedURL callers [dead?]; also fetch structuredefinition-operations.html + imposeProfile/compliesWithProfile/interface extension defs for the mi-use-* rows in ch9). J-e CARRY-INS from J-d: (1) JI-18 obligation MS-fold asymmetry — build 2-line repro before filing; (2) JI-19 xver PPP:700 raw-field — check ValidationEngine/IG publisher call setXver [outside cone; `git grep setXver HEAD`]; (3) DEV-038 empirical exhibit still to pick from the sweep (any test with a datatype type.profile + child constraints); (4) DEV-037 RFC candidate for ch15 (Extension.url fixedUri convention — write the RFC when polishing ch15 before the WGM brief). Then WGM brief (freeze ~2026-09-14). Run J-e in a FRESH session. **Packet J-d DONE 2026-09-01** (ch07 + ch03 Java sections filled; extract java-ch07-type-expansion-and-ch03-base-resolution-2026-09-01.md; NEW DEV-037 = Extension.url fixedUri synthesized by .NET only [Java grep: no `.url` backfill anywhere; ext-recursion-2 golden has zero fixedUri; ext-sort-issue's one is inherited from its base extension = base's url]; NEW DEV-038 = type-profile scope inverted [Java template = profile ROOT only for Extension/Resource-typed bases PPP:763-772, Resource roots lose constraints; datatype profile roots NEVER merged, doc override discards them PU:2643-2648; children from profile snapshot only if base has no children (`!baseWalksInto` PPP:829/1080/1423/1672) → base beats type, inverse of .NET; sliced-base new slice picks up root min/max one-directionally PPP:1398-1420, 2+ profiles → java.lang.Error]; OQ-001 Java = datatypes base wins / extensions root max survives but min→0 for open slicing PPP:801-805 / sliced base tighten-only; OQ-002 Java = base wins; OQ-016 Java consistent in both roles [no "no differential" check; root = snapshot first element PPP:754]; OQ-017 Java = profile-element by element id for template/type-check/final-sweep but NOT for children walk-in [PU:2673 todo], `#fragment` silently stripped by findProfile PU:4100-4102 → disjoint syntaxes, no shared test for either; OQ-021 type-profile dimension (d); OQ-014 J-d rows [two java.lang.Errors for author-legal input: PPP:1418 multiple profiles, PU:882 type on root]; DEV-018 Java = fragment dropped silently; DEV-019 subsumed by DEV-037; DEV-032 pinned [inherit-obligations requires same baseDefinition PU:1210; MS fold only on copy-through path PPP:1068 = JI-18; imposeProfile read only in sdConformsToTargets PU:3333]; ch3 Java: generateSnapshot takes resolved base as parameter, type mandatory on both SDs, constraint type-equality check PU:759 [t29b], unconditional on-demand base generation 762-768, fixTypeOfResourceId mutates base SD, checkTypeParameters Java-only, cloneSnapshot rewrites id+path via replaceFirst, no whole-snapshot deep copy [walk emits fresh rows], findProfile version params inconsistent [PPP template selection/min-max pickup bypass it], makeBaseDefinition vs .NET #3576 carve-out, no interface skip in cone, updateURLs list-toString latent bug PU:2143; upstream: JI-17 FILED #2596 [all 4 sites; oneMatchingElementInDifferential requires size==1 PPP:1724], JI-11 FILED #2597, JI-18/19 held needs-verification; master 4f52ba6 = PU +1 line from PU:222, PPP/PRE unchanged). **Packet J-c DONE 2026-09-01** (ch04 + ch02 Java sections filled; extract java-ch04-matching-and-ch02-preprocessing-2026-09-01.md; NEW DEV-035 = matching architecture inversion [Java walks BASE and queries diff scope via getDiffMatches PU:2444 (whole remaining scope, per-segment [x] tolerance, isSameBase symmetric); unmatched constraint rows = orphans dropped + ERROR PU:908-948, specialization rows appended PU:842-867; .NET = universal silent New; t23a = cursor-jump PPP:827 skips rows; t23 IS t23a with sort="true" → sortDifferential is Java's tooling-side normalization (driver sorts 28 tests + every base; never called by generateSnapshot); .NET driver ignores `sort`, Fix_t23 hand-swaps rows; [x]-diff vs renamed-base matched by Java not .NET; out-of-order existing slice = Java throw NAMED_ITEMS_ARE_OUT_OF_ORDER vs .NET Add]; NEW DEV-036 = sliceIsConstraining never read by Java (grep zero hits PU/PPP/PRE), enforced by .NET → OQ-006 both sides answered; DEV-012 settled [t26 sort-only on ordered input]; DEV-010 Fix_t23 explained; DEV-027 Java mechanisms pinned; OQ-014 Java rows + CORRECTION: t37 fails in Java's SORT step ("counts differ"), not with a type-naming message; OQ-015 Java = cloneDiff PU:1483 + write-back limited to derivation user data + opt-in wantFixDifferentialFirstElementType root-type clear [default false]; hasInnerDiffMatches fall-through PU:2438 = the i==start "window starts at the row itself" case, not a bug; Java has NO tree reconstruction — sparse parents handled in-walk PPP:1061-1130 [outcome-equivalent to MakeTree stand-ins, but throws where .NET makes stand-ins+New]; checkDifferential PU:1413 path grammar = Java-only [empty segment/charset/64-char/[x] placement, all throw]; closeDifferential/cleanUpDifferential = public utilities with no caller in the cone, cleanUp has hard-coded DSTU2/STU3-era discriminators; RFC-012 data point added; upstream: no new ready bugs, JI-16 [sort comparer errors debug-only PU:3916] held as design question). **Packet J-b DONE 2026-09-01** (ch05 Java section filled; extract java-ch05-merge-updateFromDefinition-2026-09-01.md; NEW DEV-034 = per-property divergence catalogue (a)–(k) [frozen-by-omission: code/representation/orderMeaning/meaningWhenMissing/defaultValue/sliceIsConstraining silently dropped by Java; isSummary change = hard `throw new Error` abort PU:3042; isModifier frozen except extension elements via checkExtensionDoco gate; restated constraint key DROPPED by Java vs .NET overlay = RFC-009 two answers; binding = rebuild w/ inherited description+extensions dropped [COPY_BINDING_EXTENSIONS=false] + required-lattice + expansion-subset enforcement; ED-extension 4-policy-list machine + duplicate-append default; example keyed label+value; label "..." append BROKEN (swapped operands mergeStrings PU:3156) while definition/comment/requirements append = byte-identical to .NET (OQ-010 answered); min/max = warn-and-take vs .NET silent-most-restrictive → 4 enforcement postures = OQ-011 WGM exhibit; fixed/pattern wholesale replace + checkTypeOk (OQ-012 answered)]; DEV-001 settled agreement-on-replace; DEV-002 pinned three-way [Java stamps inherited constraints w/ BASE SD url BEFORE merge, diff constraints never stamped; .NET stamps everything-unstamped AFTER w/ derived url]; DEV-017 pinned R5+-only [R4- = identical identity+map union; R5+ APPEND comma-merge via MappingAssistant; SD-level mapping reconciliation/renames/pruning = Java-only surface]; trimDifferential = near-dead [hard-false except PPP:1246 passes `closed`; J-a's "PPP:1335" cite was wrong; trim branches touch only the diff clone]; additional-base second merge table documented in ch5 [profile-wins fill-if-absent; min/maxLength pick LOOSER = JI-15; binding×binding = throw "not done yet"; pattern×pattern operand bug PRE:450]; upstream file now 12 ready + 3 needs-verification: NEW JI-12 label-append swap, JI-13 inverted hasAdditional guard PU:2949, JI-9/10 verified→ready, JI-14 MappingAssistant renames inversion needs-repro, JI-15 bounds-looser design question). **Packet J-a DONE 2026-08-31** (ch06 Java section filled; 3 extract files java-ch06-simplepath-slicing / java-ch06-slicedbase-and-PU-helpers / java-preprocessor-slicestuff-2026-08-31.md; DEV-020 pinned = rebuild type:$this/CLOSED/unordered at PPP:595-598 then REOPEN iff uncovered types remain PPP:646-667 ["always closed" comment overstated; sliced-base variant PPP:1584-1588 has NO reopen], entry-min raise = literal 1 PPP:615 gated min>0-slice-must-be-last, type collapse = fixedType narrowing PPP:638-645, xtension.value+shortcut prunes types instead PPP:657-663; citation corrections: obs-2b table "sum"→literal 1, au-med-k auto-added stamping = PPP:1250 NOT :343-345 [which is extension-only]; DEV-025 corrected: merge = strict fill-if-absent ×27 props, NO append, mapping/condition never propagate on match, no .copy() → instance aliasing, match-vs-inject asymmetry; NEW DEV-033 = M1q CONFIRMED code+golden [elementsMatch keys on (path mod [x], sliceName-or-null) only → cross-slice contamination + silent constraint loss, on-questionnaire exhibits, golden blesses the bug]; OQ-003 Java = rejects unless capped-to-1 or type slicing [Ewout's case is a Java carve-out]; OQ-005 Java = partial lattice incl. CLOSED→OPENATEND tolerance + rules check skipped for choice elements; OQ-018 Java = implicit type constraint applied in BOTH forms via suffix inference PPP:571-572 [.NET R5-form is the outlier]; slice Base.min = INTER-ENGINE AGREEMENT, updateFromBase PU:2004-2020 copies verbatim, ch10 updated; APPLY_PROPERTIES_FROM_SLICER=false = community decision w/ Zulip link #IG-creation "Slices not inheriting preferred bindings from root" — tension with preprocessor copy-down, OQ-020/021 material; newSlicingProcessing: library default FALSE but test driver defaults it TRUE per manifest attr [only dk1 opts out] → golden files/sweep oracle = untyped R4+ synthetic-slicer branch, plain API callers = typed branch — flag parity matters for any future harness work). Carried forward: (e) DEV-033 minimized standalone repro not built (trigger shape documented in register; on-questionnaire is the demo); (f) **Java upstream issues: GATE LIFTED + FILED 2026-09-01** — Ewout gave **standing authorization** ("you can keep posting them"): Java snapshot-generator bugs found in future packets may be filed upstream as found, no re-ask, but only after verifying the finding and re-checking citations vs current master. Filed 2026-09-01 (citations re-verified vs master `4f52ba6`, all unchanged from b06c7ee): JI-1→hapifhir#2584 (DEV-033), JI-2→#2585, JI-4→#2586, JI-6→#2587, JI-7→#2588, JI-8→#2589, JI-9→#2590, JI-10→#2591, JI-12→#2592, JI-13→#2593, JI-3+5 bundled→#2594; J-d 2026-09-01: JI-17→#2596, JI-11→#2597. Still held in `C:\Git\snapshot-spec-materials\upstream-java-issues.md`: JI-14 (MappingAssistant renames inversion — needs two-SD repro), JI-15 (additional-base bounds looser-wins — design question, maybe ask Grahame at WGM instead of filing), JI-16 (sort errors debug-only — design question), JI-18 (obligation MS fold asymmetry — needs repro), JI-19 (xver raw-field order dependence — verify production callers). Older queue unchanged: (a) default-settings .NET re-run of ext-recursion-2/logical-goo/sushi3 (DEV-029/DEV-028 caveats); (b) trace DEV-031 cdshooks path-segment drop + xt-logical resolver failure; (c) mr-type-support .NET repro (DEV-030); (d) optional report.py classifier extension. GitHub issues FILED 2026-08-26: #3589 = DEV-024, #3590/#3591 = DEV-027.
- **Java deep-read packet plan (Phase 3, after harness; question-first order, decided 2026-08-26 from orientation maps):** J-a ch06 slicing — **DONE 2026-08-31**; J-b ch05 merge — **DONE 2026-09-01**; J-c ch04 matching + ch02 preprocessing — **DONE 2026-09-01**; J-d ch07 type/extension expansion + ch03 base resolution — **DONE 2026-09-01**; J-e ch08–ch12 sweep — **DONE 2026-09-01. PHASE 3 COMPLETE.** Next = WGM brief (see NEXT UP line). All four orientation extracts in `C:\Git\snapshot-spec-materials\extracts\java-*.md`, line-verified against clone commit b06c7ee. (PHASE 2 COMPLETE 2026-08-26. Packet 6 done: ch08 .NET — expansion only when diff constrains children; deref target = CORE structure via getCoreType (absolute url#name path untraced→Phase 4); R5 §5.1.0.10 propagation NOT implemented (closest to R6 literal-path semantics — 3 versions 3 behaviors, WGM); #3177 contentReference DROPPED + type restored on expansion; eld-5 value-domain props deliberately not copied (OQ-004); ensureAbsoluteContentReferences rewrites local→absolute in constraint profiles. ch09 .NET — #1090 root from type last-segment, missing type = warning+parse-from-diff for logical only; #3576 Base carve-out warning 10017; anonymous no-type elements valid in logical models (merge as new elements); NO sdf-3 synthesis (verified absence — R4 logical models gap, Phase-4 check); interfaces = getParent skip only, no imposeProfile support. ch10 .NET — id segments use Base.Path for [x] (ids DEPEND on Base); ids always canonical, custom diff ids merged then deliberately overwritten :1063-1066 → OQ-009 .NET answered; Base: root=self, else inherit baseElem.Base / seed from baseElem; slice Base = deep-copied sliced element's Base (Base.min NOT reset — code-derived, tests disregard Base, Phase-4 verify; :2004 comment reinterpreted as event-arg); no-match → silently NO Base (sdf-8b unmet, OQ-014 row); stale NormalizeElementBase comment. ch11 .NET — expansion-depth policy = expand only where diff constrains (the real recursion terminator); one URI stack guards ensureSnapshot (NotSupported throw, stale 'Element special case' comment not implemented); getSnapshotRootElement step-4 base-chain recursion UNGUARDED (:2469 TODO 'attacker could abuse'); isValidTypeProfile has own HashSet guard → 3 behaviors for same error; root-only re-entry via RegisterSnapshotNavigator/ResolveSnapshotNavigator + CACHE_ROOT_ELEMDEF annotation (anti-DeepCopy-leak self-defense); non-reentrant per instance. ch12 .NET — issue catalog table (10002-10017 + shared Issue.*; DUPLICATE CODES 10012 and 10014 each used twice — FILED as #3587; 10003/10011 unreachable); throw catalogue; 6 settings (MergeTypeProfiles removed 2016, always on); 4 events (3 behavior-altering; PrepareBaseProfile hands out cached artifact with mutation warning); non-inheritable-extensions blocklist of 17 core urls → NEW OQ-019; CONSTRAINED_BY_DIFF_EXT uses fake hl7.org canonical 'constrainedByDifferentialExtension' (Firely-invented; 404 verified 2026-08-26 — FILED as #3588).) (Packet 5 done 2026-08-26: ch06 .NET filled — startSlice (REJECT_SLICE_NONREPEATING_ELEMENT compiled out → OQ-003 .NET answered; missing-entry issue branch unreachable via matcher; extension entry = clone+value:url/open/unordered; extension entries merged SHALLOWLY :1855-1866 → explicit entry children silently dropped → new OQ-014 row; sibling-named-entry DuplicateAfter branch = DSTU2 Composition.section legacy), addSlice (isRenamed=R4 form vs isImplicitTypeSlice=R5 form; synthesized type:$this/open entry; addSliceBase inserts pristine initSliceBase clone + unmerged CopyChildren, InsertAfter rewrites path to normalized [x]; findSliceAddPosition 2 modes: renamed-family append vs reslice-group placement; applyImplicitChoiceTypeConstraint ONLY for renamed form → R5-form slice w/o explicit type keeps full choice list → NEW OQ-018, prime WGM material; auto slice name = diff.PathName; NORMALIZE_RENAMED_TYPESLICE hard-defined everywhere → un-normalize + merger rename-merge dead, merger throws on non-choice rename), reslicing = '/'-in-name only, eld-16 never validated; verified absences: rules/ordered NEVER read (OQ-005 .NET answered; §5.1.0.17 lattice + §5.1.0.14 sums + discriminator validation all unenforced), @default unknown to generator, position/exists discriminators unsupported.) (Packet 4 done 2026-08-24: ch07 .NET filled — mergeTypeProfiles gates (1 distinct code, ≤1 distinct profile, diff≠implied base profile), full-expansion path (clone+rebase type snapshot, copyChildren, then merged AS-a-differential = the type-beats-base mechanism; acknowledged revert flaw :1405-1411 → OQ-002), root-only merge :1464-1476 (=OQ-001 entry point), expandElement paths (common-code/Element fallback, single-custom-profile-only, System.* skip), createNewElement (self-referential Base from diff min/max; removeNewTypeConstraint is DEAD CODE), fixExtensionUrl (fixedUri conventions; skips modifierExtension → DEV-019); +DEV-018 complex `url#element` expansion path — VERIFIED empirically 2026-08-24, filed as FirelyTeam/firely-net-sdk#3583, then user asked for a fix in that same session (override of the "register entries, not fixes" decision for this one bug): fixed on branch `claude/elegant-leakey-7d15e9` (commit 6883f05f2) by resolving the `#elementName` fragment via SliceName lookup (MoveToChild("extension")+MoveToNextSliceAtAnyLevel) instead of JumpToNameReference; regression test at SnapshotGeneratorTest.cs ~:4266; full SnapshotGenerator suites green on R4/R4B/R5 (apparent flakiness traced to pre-existing concurrent-test-run contention in unrelated TestExpandAllCoreTypes/TestExpandCoreArtifacts, reproduced on unmodified baseline too, not caused by this fix). STU3 shim package (`src/Hl7.Fhir.STU3/Specification/Snapshot/SnapshotGenerator.cs:1170`, ships as its own NuGet package, doesn't reference Hl7.Fhir.Conformance) has the identical bug, left unfixed — flag to user if relevant. Update DEV-018 register entry (docs/snapshot-spec branch) to "fixed via #3583", not "deviation"; +OQ-017 profile-element extension unsupported (.NET only has fragment syntax); OQ-014 rows added (silent-skip multi-profile / issue+continue / abort-children / throw-on-base-cycle).) (Packet 3 done 2026-08-24: ch02 .NET preprocessing — MakeTree 4-case algorithm, throws on no-path/multiple-roots (→OQ-014 rows), stand-in parents, shared element instances (→OQ-015 mechanism), root-sliceName repair; ch03 .NET base resolution — interface-skip in resolveBaseDefinition, #3576 Base carve-out for logical models, ensureSnapshot gates+createdBy annotation, Rebase rewrites paths only, getSnapshotRootElement 4-step cascade, no cycle detection on root base-chain (:2469 TODO→ch11); +OQ-016 differential-less SDs. Packet 1 done 2026-08-24: ch05 .NET table, DEV-017, OQ-011/012/013, RFC-008/009/010 .NET data points, DEV-001 corrected. Packet 2 done 2026-08-24: ch04 .NET matching; OQ-006 CORRECTED; +OQ-014, +OQ-015; DEV-008 enriched.)

**Goal:** write a complete specification document of FHIR snapshot generation (in `docs/snapshot-spec/`, this repo) including a .NET↔Java deviation register, as the foundation for a later fresh reimplementation of the SDK's SnapshotGenerator. Started 2026-08-21. Full plan: `C:\Users\ewout\.claude\plans\central-to-working-with-zazzy-river.md`.

**Settled decisions (user, 2026-08-21):** R5 as reference semantics with R4/R4B delta notes; STU3 fork out of scope; document lives in this repo `docs/snapshot-spec/` (multi-chapter markdown, internal tone OK); Zulip via personal API key, read-only, NEVER post (drafted questions are posted by Ewout himself); empirical Java-oracle harness (validator_cli) built early and used throughout; found bugs get register entries/issues, not fixes in old code; standing authorization to FILE verified bugs upstream: Java (hapifhir/org.hl7.fhir.core, 2026-09-01) and .NET (FirelyTeam/firely-net-sdk, 2026-09-03).

**Phase status:** Phase 0 DONE; Phase 1 (skeleton + spec baseline) DONE 2026-08-21; **Phase 4 packets 1+2+3 DONE 2026-08-26** (packet 3 = sweep mining: register grown to DEV-032/OQ-021 — DEV-021 createNewElement seeds new elements from datatype snapshot root incl. regeneration cascade [string comment → code root → Practitioner.gender; explains comment/alias/binding-ext/TYPE-CONSTRAINT ≈7k diffs; .NET-regenerated core ≠ published core]; DEV-022 copy fidelity vs Java extension filtering [OQ-019 "lists don't agree" data point]; DEV-023 contentReference abs-vs-local + survival; DEV-024 **.NET drops reslice subtrees incl. authored min/patternUri — silent data loss, strongest item**; DEV-025 materialization depth [Java preprocessor sliceStuff propagation = SnapshotGenerationPreProcessor via PU:825, fill-if-absent min/max/MS/fixed/pattern/type/binding + append constraint/example, injected elements; 77% of min/MS diffs] → OQ-021; DEV-026 renamed-choice anchoring [.NET synthesizes value[x]:valueTYPE slice, Java anchors on bare value[x] — OQ-018 enriched]; DEV-027 corrupt outputs [t23a duplicate id + fabricated base.min; obs-unit phantom element + dropped fixedString]; DEV-028 9-group validation catalogue [Java 21/21 fail tests, .NET 8/21, 13 silent]; DEV-029 recursion crossover [settings caveat!]; DEV-030 cross-version hybrids; DEV-031 cdshooks path bug untraced; DEV-032 additionalBase+obligations Java-only; DEV-003/005 settled agreement-on-outcome, DEV-007 confirmed, DEV-014 split; ch5 min/MS merge verified CLEAN [zero bucket-B]; 3 extract files element-set/failtests/min-mustsupport-2026-08-26.md; harness README sweep-health notes [t26 sort artifact, xt-logical resolver failure, report.md 40-entry cap, settings sensitivity]): batch three-way harness (BatchRunner.java = one shared-context JVM in manifest order, faithful testGen/testSort/register/getSD replication incl. setIds-on-source ordering + TestPKP replica; .NET batch process + universe resolver over all -expected files + FhirPackageSource package parity) — **full 164-test sweep: Java oracle EQUAL vs golden 143/143 gen+sort, 21/21 fail JUnit-pass** → every .NET diff is real signal. report.py = noise-classified per-property report (keyed-list alignment; 10 classes over ~52k diffs, 7974 NEW + 556 element-set = packet-3 queue). Headline classified: DEV-020/OQ-020 type-slicing entry normalization (obs-2 family gradient: Java injects type:$this always, forces closed when entry types = sliced types, collapses types + raises min when slice has min=1; .NET merges entry as written; spec favors .NET on type list, silent on rest) + OQ-018 renamed-form data point (ts-case2 identical). .NET: 0 golden matches anywhere, refuses ext-recursion-2/logical-goo (recursion guard), silently accepts 13/21 fail tests. DEV-018 register updated to fixed-via-#3583. TWO harness pitfalls burned in README: ANSI logger noise on stdout (results → dedicated tsv) and NpmPackage.fromPackage(stream)→eager-branch→TestPackageLoader.loadBundle-null silently drops all package resources (packages MUST go through ~/.fhir cache via pcm.addPackageToCache; cache now pre-populated: THO 7.3.0, uv.extensions 5.2.0, sdc 4.0.0, xver 0.1.0 — local tgzs in tools/pkgs/). **Phase 2 (.NET deep-read) DONE 2026-08-26** — all "## .NET behavior" sections of ch02–12 filled across 6 packets, OQs grown to 19 (OQ-003/005/006/009/010 .NET sides answered). **Phase 3 packet 1 (Java orientation) DONE 2026-08-26**: 4 verified structure maps in extracts (java-ProfileUtilities-map.md 225 methods / java-ProfilePathProcessor-map.md 34 methods + 7 state objects / java-SnapshotGenerationPreProcessor-map.md 44 methods / java-SnapShotGenerationTests-driver.md); key orientation finds: preprocessor does THREE jobs (slice-stuff propagation, EXT_ADDITIONAL_BASE merging w/ own per-property merge table, orphan utilities), Java-only surfaces .NET lacks (additional bases, obligation profiles, xver extensions, sortDifferential/closeDifferential, FHIRPath choice-group pruning, EXT_SNAPSHOT_BEHAVIOR extension-inheritance policy + 4 static URL policy lists), candidate Java bugs for register (mergeAdditionalBinding sets source not dest PU:3225, pattern-branch getFixed operands PRE:450, "modiferExtension" typo PRE:1078, AllowUnknownProfile doc-vs-default PU:223/448). `docs/snapshot-spec/` on branch `docs/snapshot-spec`; pushed through a05ca632f (2026-09-02, end of Phase 3) — LOCAL COMMITS NOT PUSHED unless user asks. Spec extracts (5 files, ~150KB) + Java extracts (4 files) in `C:\Git\snapshot-spec-materials\extracts\` — re-read those, not raw HTML/source, when citing. R6-build (v6.0.0-ballot4) pages in `spec-html/R6-build/`.

**Key spec-baseline insights (Phase 1):** spec defines SHAPE only, no merge algorithm (explicit inherit-if-absent only for min/max + mustSupport; ∆-additive only for constraint/condition/mapping; mapping replace-by-identity NEW in R5); omission ≠ removal [prof §5.1.0.9]; choice-type renaming REVERSED R4→R5 (R4 prescribed renamed paths, R5 requires [x]+type-slice); R5 new root-cardinality rule (type root 0..1 caps referencing element max — R4 called it irrelevant); sdf-3+8b = generator fill obligations (definition/min/max/base; logical exempt in R5, NOT in R4); element-id algorithm is fully specified [ed #id]; extension expansion + url fixing = pure tooling convention; eld-1→sdf-28 scope narrowing.

**Two audiences + RFC deliverable (user, 2026-08-21):** implementers (algorithm chapters) vs spec maintainers (shape fixes). New standing deliverable: `docs/snapshot-spec/15-spec-rfcs.md` — proposed FHIR-spec changes (errata/clarifications/additions) to post on HL7 Confluence for the ANSI process; timely because **FHIR R6 normative is being wrapped up**. Seeded 2026-08-21 with 13 RFCs from extractor findings (published-page errata, missing interpretation-table rows, replace-vs-append gaps). Rule: settled open questions graduate into RFCs; re-verify each against R6 ballot text before posting (may already be fixed). Keep shape-vs-algorithm separation: spec gets shape rules only, algorithm stays in our doc (per FHIR-13402 resolution).

**DEADLINE:** Ewout attends an HL7 WGM ~2026-09-21 and will discuss open questions in person with the relevant people (Grahame Grieve et al.) in a live evening session — Claude may be invoked during it. **WGM question brief must be frozen ~2026-09-14**: self-contained markdown, per question: context, reproducing input/test id, .NET behavior, Java behavior, spec basis, decision needed; ordered by importance. Pacing: phases 2–4 run question-first (register entries + questions over prose); oracle harness pulled forward (mismatches on the 102 never-integrated tests = demo-able WGM material); sweep Zulip/JIRA per question before freezing so nothing already-answered is asked. Chapter polish may slip past the WGM.

## Materials (all local, acquired 2026-08-21)

- `C:\Git\snapshot-spec-materials\org.hl7.fhir.core\` — sparse+shallow clone (depth 1, blob:none; deepen later with `git fetch --unshallow --filter=blob:none` if commit archaeology needed). Sparse cone: `org.hl7.fhir.r5/.../conformance/profile/` (10 files: ProfileUtilities.java ~223KB, ProfilePathProcessor.java ~105KB, SnapshotGenerationPreProcessor.java ~51KB + 7 small), `org.hl7.fhir.r5/src/test/java/org/hl7/fhir/r5/test/` (incl. `SnapShotGenerationTests.java` — capital S — the manifest driver), `org.hl7.fhir.validation/.../cli`. Also relevant, not checked out: `org.hl7.fhir.validation/src/test/java/org/hl7/fhir/conversion/tests/SnapShotGenerationXTests.java` (cross-version tests). Use `git sparse-checkout add <dir>` to extend; `git grep <pat> HEAD -- <path>` works without checkout.
- `C:\Git\snapshot-spec-materials\fhir-test-cases\` — sparse clone: `r5/snapshot-generation` (663 files, manifest 166 tests) + `r4b/snapshot-generation` (470 files).
- `C:\Git\snapshot-spec-materials\spec-html\R5\` + `R4\` — raw HTML: profiling, conformance-rules, elementdefinition (1.5MB, DOES contain "Interpretation of ElementDefinition in Different Contexts" — never trust WebFetch summaries of big HL7 pages, they truncate), elementdefinition-definitions, structuredefinition, extensibility.
- `C:\Git\snapshot-spec-materials\vendored-ids.txt` / `upstream-r5-ids.txt` — manifest id diff.

## Access methods

- **Zulip**: `bash C:\Git\snapshot-spec-materials\tools\zulip-search.sh "<query>" [stream|all] [num=30]` — creds in `%USERPROFILE%\.zulip-fhir-key` (email:apikey; key was pasted in chat 2026-08-21 → suggest regeneration when project ends). Default stream `conformance` (id 179177, primary source); pass `implementers` (huge but relevant, user-confirmed 2026-08-21) or `all` (= streams:public narrow, sweeps every public channel). All three modes verified working. Adjudication sweeps should use `all` so implementers-channel history isn't missed.
- **Confluence** (confluence.hl7.org): WebFetch gets HTTP 405 — use the in-app Browser pane instead (anonymous read works).
- **JIRA** (jira.hl7.org): spec-clarification tickets (e.g. FHIR-50391 on slicing rules) referenced from Zulip threads — adjudication source.
- **validator_cli** (Java oracle, verified from source): download `https://github.com/hapifhir/org.hl7.fhir.core/releases/latest/download/validator_cli.jar` (not yet downloaded). Invocation: `java -jar validator_cli.jar -snapshot <sources> -version 5.0 -output out.xml` (single) or `-outputSuffix <sfx>` (multiple), `-ig <package>` for deps. CAVEAT: engine sets `setAutoFixSliceNames(true)` (ValidationEngine.java:1010) — behavior difference to account for when diffing. JDK 21 installed locally.

## Key facts (from 2026-08-21 exploration; details in plan file)

- .NET engine: `src/Hl7.Fhir.Conformance/Specification/Snapshot/` — SnapshotGenerator.cs 2193 ln (generate→merge→mergeElement recursion; type-profiles-beat-base at :999; 3-step merge order at :1383; mergeTypeProfiles ~310 ln), ElementMatcher.cs 1043 ln (child matching → Merge/Add/Slice/New/Remove/Invalid), ElementDefnMerger.cs 828 ln (per-property semantics; unresolved cardinality diamond problem at :689), DifferentialTreeConstructor, SnapshotBaseComponentGenerator; infra in Hl7.Fhir.Shims.Base (recursion stack, element-id gen, annotations, events, 6 settings).
- HL7/Java shared test suite is vendored (misleadingly) at `src/Hl7.Fhir.Specification.Shared.Tests/TestData/snapshot-test/Type Slicing/` — 64 manifest entries vs upstream 166: **102 tests never integrated** (ids incl. mi-use-*, logical*, ext-recursion-*, reslicing-profile*, profile-mapping-1..4, obs-6/badfixed/badpattern, zib/uk-core/ILCore/ihe/sushi/telus); only `au1` removed upstream. Driver `SnapshotGeneratorManifestTests.cs`: 8 ids commented out, ~10 [Ignore]d vs issues #1252–#1256, FixInput()/FixManifest() rewrite checked-in HL7 fixtures at runtime (each patch = documented deviation). No golden-file diffing exists anywhere; verification is FHIRPath-rule based.
- Deviation-register seeds: issues #827 (type-list replace), #1052 (Constraint.source), #1252–#1256 (obs-*), #2466, #3177 (contentReference) + all FixInput/FixManifest patches. Java serves all FHIR versions from one R5 codebase via conversion → distinguish real deviations from version-normalization artifacts.
- Java repo has NO `org.hl7.fhir.r5/src/test/resources/snapshot-generation` (verified absent, not just empty) — the fixtures our vendored copy's header cites moved to fhir-test-cases. Verify in Phase 3 that `SnapShotGenerationTests.java` loads from fhir-test-cases.
- Only community write-up found so far: Outburn, "FHIR Packages and Snapshots: get them right!" (https://outburn.health/fhir-snapshots-and-packages/) — not yet read.

## Prior art & history finds (2026-08-21, Phase 1 session)

- **JIRA FHIR-13402 "Clarify snapshot generation rules"** (Chris Grenz 2017; Ewout commented agreement 2017-09-11; reopened 2023; closed Not Persuasive/No Change): HL7 formally acknowledges the rules are undocumented; resolution says a future write-up "will be done in Confluence, not as a formal part of the spec" → our document could become that page; strong WGM framing. jira.hl7.org readable via Browser pane (WebFetch 403s).
- **Chris Grenz FHIR-Primer wiki** cloned to `C:\Git\snapshot-spec-materials\FHIR-Primer.wiki\` (17 pages). Key prior art: Snapshots-Determining-Refines.md (9-rule refinement lineage), Aggregating-Profile-Differentials.md (5-category per-property merge taxonomy: key/immutable/aggregating/overwriting/most-restrictive), plus Slicing.md, Implicit-Type-Slicing.md, Slice-within-Slice-DSTU2.md, Type-Profiling.md. NOTE: his classification disagrees with current .NET in places (fixed[x]/defaultValue[x]/isModifier "immutable", binding "most restrictive") — question fodder, verify in Phase 2/3. DSTU2/STU3-era; treat as non-normative.
- **Confluence sweep DONE**: only 4 hits for "snapshot generation", none substantive (tooling-ecosystem mentions, 2018 FHIR-I minutes pointing at GForge 13402 = FHIR-13402 above).

## Carried-forward leftovers

- validator_cli.jar DOWNLOADED 2026-08-26 → `C:\Git\snapshot-spec-materials\tools\validator_cli.jar` (188MB), smoke-tested OK on JDK 21: **v6.10.2, Git# d06577dbc5c6, built 2026-08-13**. CAVEAT: that's the 6.10.2 release commit, NOT our source clone b06c7ee (master 2026-08-21) — record this pair in every harness diff; if a mismatch looks commit-sensitive, check whether the code changed between them (`git log d06577dbc5c6..b06c7ee -- <file>` needs unshallowing first).
- `defining-extensions.html` downloaded (R5 + R6-build) but NOT yet extracted — extension-context semantics live there; needed for ch7/ch9 completion.
- Never downloaded (tier-B fetch candidates from the structuredefinition extract): `structuredefinition-operations.html` ($snapshot operation) and the imposeProfile / compliesWithProfile / structuredefinition-interface extension definitions — Phase 3 needs these for the `mi-use-*` tests.
